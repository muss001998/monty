#include "monty.h"
/**
 * f_stack - this will print the top
 * @head: this reps the stack head
 * @counter: this is the  line_number
 * Return: this will have no return
*/
void f_stack(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	bus.lifi = 0;
}
